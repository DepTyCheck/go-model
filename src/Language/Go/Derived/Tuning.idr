module Language.Go.Derived.Tuning

import public Language.Go

import Deriving.DepTyCheck.Gen

-- export
-- GenOrderTuning "DeclareVar".dataCon where
--   isConstructor = itIsConstructor
--   deriveFirst _ _ = [`{name}, `{new}, `{ty}, `{initial}, `{add}]
